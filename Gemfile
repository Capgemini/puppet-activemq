source 'https://rubygems.org'

puppet_version = ENV.key?('PUPPET_VERSION') ? "= #{ENV['PUPPET_VERSION']}" : ['>= 2.7']

gem 'rake'
gem 'rspec'
gem 'rspec-puppet'
gem 'puppet', '>= 4.10.1', puppet_version
gem 'puppetlabs_spec_helper'
